version: '3.8'

services:
  calculatorpro:
    build: .
    container_name: calculatorpro
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - FLASK_APP=app.py
      - PORT=5000
    restart: unless-stopped
